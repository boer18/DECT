# 表格工具帮助手册

这是一份面向策划和配置表使用者的快速手册。应用内可以从 macOS 顶部菜单栏的“帮助”打开同样的内容。

当前版本 1.8.12 支持 tapcoloroasis 及同类旧版 Luban 工程；导表时会自动选择项目工作流脚本并识别项目的 .NET Runtime 要求，进行进程级兼容处理。更新后首次启动会显示本次版本的功能与 Bug 说明。

## 1. 开始使用

第一次打开工具时，点击顶部“选择 Project 目录”，选择包含一个或多个工程的目录。工具会递归寻找包含以下文件的配置入口：

```text
gen.sh、gen_plus.sh 或 gen_for_command.sh
luban.conf
```

表格根目录由 `luban.conf` 的 `dataDir` 决定。

传统工程通常是：

```text
<项目目录>/Config/gen.sh
<项目目录>/Config/luban.conf
<项目目录>/Config/Datas/
```

TCR 这类工程可以是：

```text
<仓库>/trunk/LubanConfig/gen.sh
<仓库>/trunk/LubanConfig/luban.conf
<仓库>/trunk/LubanConfig/Datas/
```

tapcoloroasis 这类工程可以是：

```text
<项目目录>/Config/gen.sh
<项目目录>/Config/luban.conf
<项目目录>/Config/Luban/Luban.runtimeconfig.json
<项目目录>/Config/Datas/
<项目目录>/ColorOasis/Assets/BundleRes/Config/game/
```

工具会把最近的 `.git` 根目录作为项目身份，把选中的配置脚本所在目录作为配置根目录，把 `dataDir` 解析出的目录作为配置表数据根目录。同一工程同时存在多个脚本时优先选择 `gen_plus.sh`，因此配置目录不必固定命名为 `Config`。

项目列表会保存到本机。重新打开工具时会优先读取缓存，新增、删除或移动工程后点击“重新扫描工程”。

## 2. 浏览配置表

- 左侧搜索框支持按表名或相对路径查找。
- 点击星标收藏常用表，再次点击可以只显示收藏。
- 多表并排关闭时，单击表格只切换当前预览，不会不断增加标签。
- 双击列表中的表格会用系统默认表格应用打开原文件。
- “添加表格”可以打开其他项目中的同名表或外部表格。
- 工具会定时检查已打开文件的外部变化。没有未保存编辑时自动刷新；有未保存编辑时会保留本地内容并提示冲突。

## 3. 编辑、复制和填充

### 编辑单元格

- 单击单元格后直接输入，会替换原内容；连续输入文字、数字或中文会继续追加。
- 双击单元格进入带光标的编辑状态，按 Enter 提交；按 Shift + Enter 在当前光标处插入换行并继续编辑。
- 公式单元格未选中时显示工作簿保存的计算结果，选中后显示原始公式，可以直接修改。
- 工具不提供公式计算引擎。修改公式后，需要由支持该公式的表格应用重新计算。

### 复制内容

- 拖选矩形区域后按 `⌘C`，在目标单元格按 `⌘V`。
- 点击行号可以选择整行，点击列字母可以选择整列。
- 复制的是单元格内容和公式，不是整个表文件。
- 跨项目迁移时，打开源表和目标表，复制需要的区域，再保存目标表。

### 右下角填充柄

选中一个单元格或矩形区域后，拖动选区右下角的小方块即可填充。拖动过程中会显示完整待填充区域。

- 普通文本默认复制。
- 纯数字、日期和文字开头、中间或结尾带编号的内容会自动按序列延续。
- 多个起始单元格会按各自所在列独立判断。
- 公式会按相对引用规则调整，绝对引用保持不变。
- 松开后，填充柄旁的“填充选项”可以展开为“复制单元格”或“以序列方式填充”。不展开时使用自动判断。
- 单次填充最多 50,000 个单元格。

### 插入和删除行列

- 右键行号后，菜单会直接显示“在上方插入行”和“在下方插入行”，每一行旁边都有数量输入框和步进器；右键列字母时同样显示“在左侧插入列”和“在右侧插入列”。
- 同一个菜单还提供“删除行”和“删除列”。删除从当前右键所在的行或列开始，数量默认是 1，也可以直接填写多个；点击菜单文字立即执行，不会再弹出二级窗口。
- 被删除区域的未保存编辑会清理，后方的数据和未保存编辑会向前移动；引用被删除行列的公式会写成 `#REF!`，其他引用按新的坐标移动。
- 插入、删除都支持保存、撤销和重做。工具不会自动生成备份，保存前如需历史副本请先自行复制 XLSX。

### 视图调整

- 使用 `−`、`+` 和“重置大小”调整表格缩放。
- 触控板捏合，或按住 `⌘`/Control 滚轮，也可以缩放表格。
- “自适应”可以适配列宽、行高和换行；长文本不会把整张表撑得过大。
- 冻结指定行和列只影响工具内视图，不会修改 Excel 文件本身的冻结设置。

## 4. 多表并排

多表并排默认关闭。

- 关闭时，表格以单页预览方式显示，顶部标签用于切换已打开表格。
- 开启后，可以在右侧持续添加表格，当前激活面板显示蓝色边框。
- 每个面板有独立的横向和纵向滚动，不会带动整个工具栏。
- 普通滚轮滚动已加快；按住 ⌘/Control 时，滚轮向上放大，向下缩小。多表并排时只滚动当前表格，并会尽量保持长表底部的编辑位置。
- 不同项目的同名表通过完整路径区分，可以同时打开。
- 点击标签上的叉只关闭该表格；有未保存修改时会先询问保存方式。

## 5. 多语言工具

多语言工具读取当前项目的主 `TbLanguage.xlsx`，不会把框架或故事语言表误当成主表。

第一次执行翻译功能时，工具会提示打开“翻译 API 设置”。每位使用者都需要填写本工具自己的 API Key：

- API Key、API URL 和 Model 都保存在本工具自己的本机应用设置中；
- 这些设置会在应用重启后继续保留；
- 工具不会读取或复用 TCST 多语言工具的 API Key；
- 没有 Key 时，翻译和一致性检查会自动打开设置窗口。

顶部操作：

- “翻译全部缺失”：只处理空白的语言单元格；
- “保存全部”：直接写回 `TbLanguage.xlsx`，不生成备份；
- “翻译并导表”：翻译完成后保存并执行当前工程导表脚本；
- “AI 一致性检查”：检查译文和原文的含义、占位符及标签，不自动修改表格。

翻译结果会先保存为本机草稿。任务中断、应用重启或网络失败后，可以继续处理剩余内容。保存前如果原语言表被其他程序修改，工具会拦截写入并保留草稿。

## 6. 表格对比

### 文件夹对比

选择历史版本目录和当前版本目录，工具会递归匹配全部表格。选中文件后，可以切换工作表查看完整配置表。

- 红色：删除；
- 绿色：新增；
- 紫色：修改；
- 有变化的行号和列号也会同步着色；
- 左侧可以搜索表名或路径，右侧可以搜索当前 Sheet 的单元格内容、公式或位置，并显示匹配数量。
- 对比表支持拖动列标题右侧边界调整列宽；“上一个改动”和“下一个改动”会循环定位当前 Sheet 的差异并自动滚动到目标单元格。
- 详情顶部显示当前表新增、删除、修改和总差异数量；多个 Sheet 以页签切换，有差异的页签会显示对应颜色标记。
- 支持缩放、重置大小、逐文件标记“已检查”和导出完整 CSV 报告；全部核对完后点击顶部“核对完成”，会释放本次对比缓存，避免长时间占用内存。

### Git 历史对比

Git 历史模式会跟随顶部当前项目读取本地 `.git`、分支和配置表提交。可以按提交或时间选择历史版本，再与当前工作区比较，当前未提交的表格修改也会参与比较。

工具不会执行 checkout、pull、reset 或修改项目。历史表会解包到系统临时目录，对比结束后自动清理；对比工作区是只读的，不会写入历史目录或当前源表。

## 7. 一键导表

选择项目后点击右上角“导表”。如果当前项目在工具内有未保存的表格或多语言修改，工具会先保存，再运行该项目的配置脚本；tapcoloroasis 会优先运行 Unity 编辑器菜单使用的 `gen_plus.sh`，保持 JSON 产物格式。

对于 tapcoloroasis 这类 `Luban.runtimeconfig.json` 请求 net7.0 的项目，工具会读取该文件，并仅为本次导表进程设置 `DOTNET_ROLL_FORWARD=Major`，使其可以在已安装 .NET 8 或更新版本的 Mac 上运行。已经使用 net8.0 或更新版本的旧项目不会设置这个覆盖项；工程脚本、源表目录和导出目录都不会被工具改写。

tapcoloroasis 的 `gen.sh` 是二进制生成流程，误执行会把已提交的 JSON 替换成 `.bytes` 并改写整批生成 C#；工具会选择该项目实际使用的 `gen_plus.sh`，避免这类整库格式切换。

日志会显示：

- 工程目录；
- 配置目录；
- 配置表数据目录；
- 实际脚本路径和执行目录；
- 标准输出和错误输出；
- 成功、停止或失败状态及退出代码。

工具不需要启动 Unity。若日志提示 dotnet、Luban.dll、.NET Runtime 或权限不存在，请根据日志中的实际路径修复工程环境。

## 8. 顶部系统菜单和设置

macOS 顶部菜单栏中的“表格工具”菜单提供：

- 选择 Project 目录；
- 重新扫描工程；
- 翻译 API 设置；
- 版本与更新说明；
- 检查更新。

顶部“帮助”菜单可以打开本手册。系统“设置表格工具…”入口也提供翻译 API 设置，不需要先进入多语言页签。

工具会自动检查 GitHub 正式 Release。发现新版本后，会显示版本号、Build 和更新说明；确认没有未保存内容或运行中任务后，可以点击“安装并重启”。如果当前旧实例仍在运行，请先退出旧实例再重新打开新版本。

## 9. 常见问题

### 找不到工程

确认选择的是包含工程的 Project 根目录，而不是某个工程内部的 `Config` 或 `LubanConfig` 目录。工程需要有 `gen.sh`、`gen_plus.sh` 或 `gen_for_command.sh` 中至少一个，同目录有 `luban.conf`，并且 `dataDir` 指向实际存在的目录。

### 找不到多语言表

工具会优先寻找数据根目录下的主 `TbLanguage.xlsx`，找不到时再递归寻找同名主表。框架和故事语言表不会被误当成主表。

### 表格显示不完整

使用缩放和“重置大小”，再通过“自适应”调整行高、列宽和换行。极长文本可以在内容栏展开查看全文。

### 更新后没有自动重启

安装过程不会强制结束正在运行的实例，以避免丢失未保存内容。退出当前表格工具后，从“应用程序”或程序坞重新打开即可。
